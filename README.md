# Bifrost

Cross-chain best-route router (v1: Ethereum Mainnet + Base, direct non-custodial execution).

See `architecture.mmd` for system diagram, `tasks/00-overview.md` for scope, and `docs/intent-model-B.md` for the deferred intent/solver design (v2, +HYPE/HyperEVM).
